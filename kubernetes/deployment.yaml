apiVersion: apps/v1
kind: Deployment

metadata:
  name: loan-default-api

spec:
  replicas: 2

  selector:
    matchLabels:
      app: loan-default-api

  template:
    metadata:
      labels:
        app: loan-default-api

    spec:
      imagePullSecrets:
        - name: artifact-registry-secret

      containers:
        - name: loan-default-api

          image: europe-west1-docker.pkg.dev/valid-design-385517/mlops-repo/loan-default-api:8a5f932bb6e31ef0992d8b066bc972902a811ded

          imagePullPolicy: Always

          ports:
            - containerPort: 5000

          env:
            - name: GCP_BUCKET_NAME
              value: "bigdata-mlop-bucket"