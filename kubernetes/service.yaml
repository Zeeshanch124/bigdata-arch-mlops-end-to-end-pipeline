apiVersion: v1
kind: Service

metadata:
  name: loan-default-api-service

spec:
  type: NodePort

  selector:
    app: loan-default-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080